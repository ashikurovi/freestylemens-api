import { Repository } from 'typeorm';
import { CreateInvoiceDto } from './dto/create-invoice.dto';
import { UpdateInvoiceDto } from './dto/update-invoice.dto';
import { InitiateBkashPaymentDto } from './dto/initiate-bkash-payment.dto';
import { BkashCallbackDto } from './dto/bkash-callback.dto';
import { BankPaymentDto } from './dto/bank-payment.dto';
import { Invoice } from './entities/invoice.entity';
import { SystemUser } from '../systemuser/entities/systemuser.entity';
import { ConfigService } from '@nestjs/config';
import { SystemuserService } from '../systemuser/systemuser.service';
export declare class InvoiceService {
    private invoiceRepository;
    private systemUserRepository;
    private configService;
    private readonly mailerTransport;
    private readonly systemuserService;
    private bkashAppKey;
    private bkashAppSecret;
    private bkashUsername;
    private bkashPassword;
    private bkashBaseURL;
    private bkashGrantTokenURL;
    private bkashCreatePaymentURL;
    private bkashExecutePaymentURL;
    constructor(invoiceRepository: Repository<Invoice>, systemUserRepository: Repository<SystemUser>, configService: ConfigService, mailerTransport: any, systemuserService: SystemuserService);
    create(createInvoiceDto: CreateInvoiceDto): Promise<Invoice>;
    findAll(): Promise<Invoice[]>;
    findOne(id: number): Promise<Invoice>;
    findByCustomer(customerId: number): Promise<Invoice[]>;
    findByInvoiceNumber(invoiceNumber: string): Promise<Invoice>;
    update(id: number, updateInvoiceDto: UpdateInvoiceDto): Promise<Invoice>;
    getBkashToken(): Promise<string>;
    initiateBkashPayment(initiatePaymentDto: InitiateBkashPaymentDto): Promise<any>;
    executeBkashPayment(paymentID: string): Promise<Invoice>;
    processBkashCallback(bkashCallbackDto: BkashCallbackDto): Promise<Invoice>;
    processBankPayment(bankPaymentDto: BankPaymentDto): Promise<Invoice>;
    verifyBankPayment(invoiceId: number): Promise<Invoice>;
    rejectBankPayment(invoiceId: number, reason?: string): Promise<Invoice>;
    remove(id: number): Promise<void>;
    private generateInvoiceNumber;
    private generateTransactionId;
    private handleInvoicePaid;
}

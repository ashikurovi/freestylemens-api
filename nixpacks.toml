[variables]
NPM_CONFIG_PRODUCTION = "false"
NPM_CONFIG_LEGACY_PEER_DEPS = "true"
[phases.install]
commands = ["npm ci --legacy-peer-deps --include=dev || npm install --legacy-peer-deps"]

[phases.build]
commands = ["npm run build"]

[phases.start]
commands = ["npm run start:prod"]
